const {{name}} = {
	{{#if state}}
	state: {},
	{{/if}}
	{{#if mutations}}
	mutations: {},
	{{/if}}
	{{#if actions}}
	actions: {},
	{{/if}}
	{{#if getters}}
	getters: {}
	{{/if}}
}

export default {{name}}
